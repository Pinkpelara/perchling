# Handoff

## State
v0.29.1 (2026-09-18): her first test of 0.29.0 with everything owned: "they do a lot of napping and not the other tricks I pick" and "unable to uncheck peekaboo". The signature move was pinned on (and re-added at every load): now any pick switches off. _choose weights: trick 6 + half the tricks on (max +16), sleep 6, Sleepy +10 (was +18), bedroom naps 30% of sleeps and 60-120 s (was 60% and 90-200 s). Full harness 226/226.
v0.29.0 (2026-09-17, full harness 213/213 alone, committed and pushed): the cursor reactions she asked for on 2026-09-17 are built per docs/superpowers/specs/2026-09-17-cursor-reactions-and-house-drag-design.md: dangle poses + parachute (pets.js makeParachute, sprite.html ?prop=, render_sprites.py --prop, assets/sprites/_props/parachute_sheet), dizzy mood (spiral eyes) rendered into all four sheets, watch_spin/go_dizzy with star Overlay, held->chute->float->land, drop on the house (house.json open_box/rooms), drag a pet out of an open room (house pet_boxes/pet_drag, house-out file with x,y, come_out(at=)), two-level bars (7/2s nod, 15/5s or 15 clicks/10s line), menu page Cursor tricks, site notices, README section. Part 1: 178/178; house checks 11/11; full 213/213.
Before it v0.28.3 released 2026-09-17 (commits eb2db17, 896f23e; CI green; site live): menu tips fixed (tip() bound only the tile frame, Tk fired Leave on crossing to the label; now one Tip per card, bound on tile + children) and on every sub-page tile and in Choose tricks; errands (walk to the house door) survive a reaction/tickle/drag via set_aside_errand/carry_on; cursor glance mid-move; site skills block generated by tools/build_skills.py (39 tricks, habits, company, 10 games, notices, moods), sulk is one hour everywhere. Harness: part 1 160/160, full 189/190 (one unexplained flake: leaf asleep didn't join peekaboo; isolated repro joins fine).

## Next
1. She should try all four gestures on her installed 0.29.0 (it arrives within a minute of the release; a restart of one pet if not). Watch for false dizziness from ordinary mouse use (the bar is 2.5 turns in 2 s within 2.6 pet sizes, cursor motion only) and for the chute over a pet dropped on another monitor.
2. Her "features we built along the way are completely missing from the menu" turned out to be these four (never built until now, she was waiting for them). Still worth a quick audit of page_home against README.md in case she meant more. Start by asking which ones she looked for, then audit app/menu.py page_home against everything in README.md and shop.json. Likely candidates: tiles that only appear conditionally (Play with the others needs another pet out on the SAME screen, else the inert "No one else is out"; Go inside only when the house is on this screen; Decorate/styles only inside The house page; Effects only inside Closet; Come out/Stop dancing replace Hide/Dance); features from the 0.14 ranked list never built (visiting, anniversaries, yard, trading beyond hat codes). Her standing rule: every feature visible; consider showing every tile always, greyed with a one-line reason, instead of hiding it.
3. Owner-side blockers to keep raising: Lemon Squeezy checkout link + variants map in shop.json, Steamworks, domain; refund policy undecided (never promise one).

## Context
Run everything with C:\Users\pourkam1.ARTSCI\AppData\Local\Programs\Python\Python312\python.exe (shell python is 3.11). Never run two harnesses or a scratch Driver at once: they share .testrun/appdata (the harness now takes a Perchlings-testrun mutex and kills leftover test pets at start; a harness that dies mid part 2 leaves its pets, house and stage running, and Tk objects finalized by a watcher thread crash Tcl, so part 1b gc.collects on the main thread). Test hover with a gliding real pointer, not generated events. The harness runs while she uses the PC: part 1 unbinds the test pet's mouse, the real-pointer glide runs only when the PC is untouched, injected bursts skip when GetAsyncKeyState never saw them. Her installed 0.28.3 checks for updates every minute (one ETag lookup per household). When she says "X doesn't work" she means the app unless she says site. House style: no em dashes, no exclamation marks, no counts, no "unlock", no refund promise, Roblox/Adopt Me register, humanizer before committing prose; pets $1.99, add-ons $0.99, no bundles.
