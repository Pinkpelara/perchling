# Handoff

## State
v0.28.2 (2026-09-17): the user's running pets never offered the new releases ("the update is not picked up by pet menu", then "any update should become available promptly no time limit"): the app checked only at start and every 6 h. Now every minute (UPDATE_EVERY) and on every panel open (10 s gap); one look per household (look_for_update writes household/update.json with tag, etag, ts; the request sends If-None-Match and a 304 is free of GitHub's 60/h limit; offline keeps the last known tag); the open panel redraws with the Update line; worker threads hand Tk work to Pet.later() drained by the tick (root.after from a thread failed outside mainloop, which also hid the check in the harness). Verified live against GitHub (200 with ETag, then 304). Her currently running pets still have the 6 h check: quit and start one pet once (or use Update when it shows) to get 0.28.2. Harness lesson: never run ad-hoc Driver scripts on .testrun/appdata while the harness runs; they destroyed its root and failed two checks that were fine alone. Before it v0.28.1 (2026-09-17): the systematic audit (/superpowers:systematic-debugging, "audit the entire app and fix bugs"): five bugs reproduced first, fixed at the root, each with a harness check: (1) a second pet only nodded when another pet answered the same burst first; (2) panel timers outliving a closed window fired into a reused Tk command; (3) closing the panel wiped the shop's global wheel binding; (4) a damaged state file crashed the pet at start (atomic writes + .bak + safe reads now; let_go deletes the .bak); (5) a reminder due while inside the house was said by a bubble with no pet. Also: the real-burst checks in part 2 skip on a locked or untouched PC (injected keys don't reach a locked desktop; that was the 171/173 run). Before it v0.28.0 (2026-09-17): the app audit the user asked for ("reactions do not work properly ... some react too late some do not react at all and usually they do not react if they are in the middle of a trick", "gossip mode says random things", "some menu items are not clear"). Reactions: reactive() returns full/say/None, interrupt() drops the pet's own activity, bars lower (7 keys in 2 s), cooldowns 60 s or less with a nod in between, away/back from the PC's idle clock (3 min away, 12 min nap, hello on the first tap, household-wide), the lock screen as away, a look up when the cursor lands, head turned toward a near cursor, nudge at 20 min, sulk at 60 min, time away not counted as ignoring. Commands: ready() ends whatever the pet is doing before any menu action (no more "In a minute."). Menu: TIPS on every tile, "Keep you company", "Choose tricks". Gossip: about the owner first (every note used or quoted, places, jobs, wants, things, the day's memory: tickles, tricks asked, cheers, minutes away), filler last; pronoun after the name. Harness: 21 new checks in part 1 (145 total), part 2's burst test taps once first so the away/back hello isn't mistaken for the cheer. Before it v0.27.0 (2026-09-16): the site rebuilt as a product page after the user said the humanized rewrite was only cosmetic and asked for a million-dollar-business site learned from competitors and award-winning pages (epic design, 3D, human language, best seller; audience adults and teens, not kids only). Research: a Superdesign canvas draft (project 18edb038-a80d-471c-b7e6-9baf7a62de11) as a layout sketch, plus a research-and-copy workflow (award pages, desktop-pet competitors incl. Steam reviews, impulse pricing, sentence audit; three writers, two judges, a scrub) whose winner is merged into site/index.html. Page: live 3D hero (pets say real lines, wave, free hats on click), one literal headline, price in the button, true-size desktop clip first, nine moments one picture each, quote wall, $1.99 card + $0.99 shelf, steps, five FAQs, closing ask, sticky bar; 993 words, no counts. Clips: build_showcase.py renders 2x at 24 fps to MP4 with 10 fps GIF fallback (R, N(), save_mp4, desk(), stage at 2x). Prices: pet $1.99 everywhere, every add-on $0.99, picks:all bundle removed. Structure rules in docs/competitor-language.md. Before it v0.26.4 (2026-09-16): the user set the rule for hatched pets: they are minis (pocket-size for good, E.MINI_SCALE 0.72), a household can raise at most 6 minis (E.MAX_MINIS, counted by hatched_ids()), and the pets people buy never count toward any cap, because a cap on bought pets would hurt sales. Egg page, adoption window blurb, site index, demo page lede, README, demo captions and the harness (three new checks) all say so. Before it v0.26.3 (2026-09-16): the user asked how the egg works and for a full demo; tools/demo_egg.py runs the whole egg story on the real code in .demo/egg (a backdrop window of its own, captures only its own windows) and writes a captioned storyboard and a GIF; the demo exposed a Pets page bug (a tile read 'Lilac Pink: out: on' and a long name's second line was cut off), fixed in menu.py grid(): tiles take an optional status word and grow a line for long labels. Before it v0.26.2 (2026-09-16): reactions and emotions made consistent across the household after the user saw them work "randomly on and off" (on her PC Rocco had Reacts off and Moss had Music off as per-pet toggles). Reacts and Music are household switches in owner.json now (Pet.flag / set_flag, default on); a typing burst, undo run, save run or click burst is a household event (household/react.json, shared per-kind cooldowns, every free pet joins via follow_reaction); the hover-and-leave bit is deterministic for all four characters; the dance schedule is shared by the wall clock (dance_slot); the birthday is household-wide. Test harness: PERCH_EXTRA_KEYS=7E, a real F15 burst in part 2 reaching three real pets at once; full run 151/151. Before it v0.26.1 (2026-09-16, commit 1819c2a): the site and the app's longer lines rewritten in the voice the user asked for (explaining it to a friend over coffee; adults, not kids; rules in memory feedback_perchlings_site_voice.md). Before it v0.26.0 (0b558bc): the pet's panel carries everything (house page: bring out / open / decorate / style / everyone out / put away; breaks by kind, nap, dance, party, egg page), the house decorates with pictures and has its own card, pets drive the house via household/commands/house.json, reactions fire in more states with lower bars, dances run in sets, bedroom naps are rate-limited, lowercase owner names work. Site: routine.gif and breaks.gif rows, "one right-click away", demo page screenshots. Full test run + house checks green. Before it v0.25.0 (picks are owned, not capped) and the sales-page site (792c484, pushed this session).

## Next
0. Two small site commits (4ef8a6e contrast, 9aa5ca0 the review panel's fixes) may still be waiting on the user's GitHub sign-in; `git log origin/main` tells. Push them first.
0b. The site is a product page now (see README, The site); keep it that way: no counts, one picture per moment, spoken sentences, the pets' verbatim lines as the only jokes, taskbar by name. Structure rules in docs/competitor-language.md.
1. If the user reports a line that still reads wrong, fix that line in site/index.html (or the bio in app/species/*.json) and push; do not restructure the page.
2. Batch 3 of the catalog: house styles (castle, cabin, spaceship) in web/house.js + render_house.py, and a yard; then seasonal shelves and a pet maker.
3. Owner-side blockers to keep raising: the Lemon Squeezy checkout link (set site.js buyUrl, demoAdopt false; the demo checkout hands out installers for $0), the Steamworks account (tools/steam/ is ready), a domain instead of pinkpelara.github.io.

## Context
- Run the app and tools with system Python 3.12 (AppData\Local\Programs\Python\Python312\python.exe); the shell's default python is 3.11 and lacks the libraries.
- Never start a second headless Chrome on tools/sprites/_chrome-profile while a render runs; renders take ~4 s/frame when the user's pets are running.
- Never view live screen grabs; verify captures by pixel comparison. Screenshots of my own test windows (panel, decorate dialog) are fine and feed site/img/demo via assets/sprites/_fit/_menu_home.png, _decorate.png, _menu_house.png.
- House style: no em dashes or exclamation marks in app/site prose, US spelling on the site, humanizer before committing prose; site copy follows docs/competitor-language.md; never handle GitHub tokens (pushes may wait on the user's browser sign-in; cached creds worked on 2026-09-16).
- Test pets run from source with APPDATA at .testrun/; kill stray pythonw perchling.py processes before a run. The user's real pets run from the installed exe with the real APPDATA; their one-copy mutexes are Perchlings-<id>-d0d48cdf. The user had put the house away; "Bring out the house" on any pet's panel brings it back.
